
Data type changes: bigint NOT NULL auto_increment -> bigserial, mediumtext -> text, blob->bytea, enum -> varchar or equivalent.
# Change usage of \' to \'\', or use E\'\' syntax.
#
# pleasewait.starttime has 'on update' trigger. 
# MemberEmailPayload uses ENUM.